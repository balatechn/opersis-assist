# ── Opersis Assist Agent Configuration ──

# Server URL (WSS for production, WS for dev)
SERVER_URL=ws://localhost:4000/ws/agent

# Agent secret (must match server AGENT_SECRET)
AGENT_SECRET=change-this-agent-secret-key-64-chars-minimum

# Telemetry interval in milliseconds
STATS_INTERVAL=5000
